# Config for sway
#
# Read 'man 5 sway' for a complete reference.

# User config directory

include $HOME/.config/sway/conf.d/autostart_applications
include $HOME/.config/sway/conf.d/conf
include $HOME/.config/sway/conf.d/output
include $HOME/.config/sway/conf.d/input
include $HOME/.config/sway/conf.d/theme
include $HOME/.config/sway/conf.d/application_defaults
include $HOME/.config/sway/conf.d/clamshell

# only enable this if every app you use is compatible with wayland
# xwayland disable
